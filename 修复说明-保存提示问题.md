# 共借人和担保人页面保存提示问题修复

## 问题描述

共借人和担保人页面保存时，虽然后端返回 `success: true`，但前端却提示"保存失败"。

## 问题原因

### 根本原因

`request.js` 中的请求方法返回的是**完整的响应对象**，而不是响应数据本身。

```javascript
// request.js 中的代码
success: (res) => {
  if (res.statusCode >= 200 && res.statusCode < 300) {
    resolve(res);  // ❌ 返回的是整个响应对象
  }
}
```

### 数据结构对比

**实际返回的数据结构：**
```javascript
{
  statusCode: 200,
  data: {
    success: true,
    message: "新增成功",
    data: { id: 1, ... }
  }
}
```

**前端错误的判断逻辑：**
```javascript
result = await req.post('/api/xxx', data);

if (result && result.success) {  // ❌ 判断 result.success（不存在）
  // 保存成功
}
```

**正确的判断逻辑：**
```javascript
response = await req.post('/api/xxx', data);
const result = response.data;  // ✅ 先取出 data

if (result && result.success) {  // ✅ 判断 result.success（存在）
  // 保存成功
}
```

## 修复内容

### 1. 共借人页面 (`co-borrower/index.js`)

**修复前：**
```javascript
try {
  const req = wx.$request;
  let result;
  
  if (this._coBorrowerId) {
    result = await req.put(`/public/orders/${orderId}/coBorrower/${this._coBorrowerId}`, coBorrowerData);
  } else {
    result = await req.post(`/public/orders/${orderId}/coBorrower/add`, coBorrowerData);
  }
  
  wx.hideLoading();
  
  if (result && result.success) {  // ❌ 错误：result.success 不存在
    // 保存成功
  }
}
```

**修复后：**
```javascript
try {
  const req = wx.$request;
  let response;
  
  if (this._coBorrowerId) {
    response = await req.put(`/public/orders/${orderId}/coBorrower/${this._coBorrowerId}`, coBorrowerData);
  } else {
    response = await req.post(`/public/orders/${orderId}/coBorrower/add`, coBorrowerData);
  }
  
  wx.hideLoading();
  
  // ✅ 正确：先取出 response.data
  const result = response.data;
  
  if (result && result.success) {  // ✅ 正确：result.success 存在
    // 保存成功
    wx.showToast({
      title: result.message || '保存成功',  // 显示后端返回的消息
      icon: 'success'
    });
  }
}
```

### 2. 担保人页面 (`guarantor/index.js`)

**修复内容：**

1. **修复数据变量名**
   - 修复前：`coBorrowerData`（错误，这是担保人页面）
   - 修复后：`guarantorData`（正确）

2. **修复接口路径**
   - 修复前：`/public/orders/${orderId}/coBorrower/add`（错误，调用了共借人接口）
   - 修复后：`/public/orders/${orderId}/guarantor/add`（正确，调用担保人接口）

3. **修复ID变量名**
   - 修复前：`this._coBorrowerId`（错误）
   - 修复后：`this._guarantorId`（正确）

4. **修复响应数据处理**
   - 修复前：直接判断 `result.success`
   - 修复后：先取 `response.data`，再判断 `result.success`

5. **修复本地存储键名**
   - 修复前：`orderFormData_step3`（共借人的键名）
   - 修复后：`orderFormData_step4`（担保人的键名）

**完整修复代码：**
```javascript
// 构建担保人数据（不是共借人数据）
const guarantorData = {
  borrowerType: this.data.borrowerType,
  // ... 其他字段
};

// 保存到本地存储（使用正确的键名）
wx.setStorageSync('orderFormData_step4', { guarantor: guarantorData });

try {
  const req = wx.$request;
  let response;
  
  // 使用正确的ID变量和接口路径
  if (this._guarantorId) {
    response = await req.put(`/public/orders/${orderId}/guarantor/${this._guarantorId}`, guarantorData);
  } else {
    response = await req.post(`/public/orders/${orderId}/guarantor/add`, guarantorData);
  }
  
  wx.hideLoading();
  
  // 正确处理响应数据
  const result = response.data;
  
  if (result && result.success) {
    logger.info('[担保人] 保存成功', result);
    if (result.data && result.data.id) {
      this._guarantorId = result.data.id;  // 使用正确的ID变量
    }
    wx.showToast({
      title: result.message || '保存成功',
      icon: 'success'
    });
    setTimeout(() => {
      wx.navigateBack();
    }, 1500);
  }
}
```

## 后端接口返回格式

### 共借人接口

**新增：** `POST /public/orders/{orderId}/coBorrower/add`

**更新：** `PUT /public/orders/{orderId}/coBorrower/{id}`

**返回格式：**
```json
{
  "success": true,
  "message": "新增成功",
  "data": {
    "id": 1,
    "borrowerType": "personal",
    "name": "张三",
    "idCardFront": "http://localhost:8081/uploads/1/coBorrower-idFront.jpg",
    ...
  }
}
```

### 担保人接口

**新增：** `POST /public/orders/{orderId}/guarantor/add`

**更新：** `PUT /public/orders/{orderId}/guarantor/{id}`

**返回格式：**
```json
{
  "success": true,
  "message": "新增成功",
  "data": {
    "id": 1,
    "borrowerType": "personal",
    "name": "李四",
    "idCardFront": "http://localhost:8081/uploads/1/guarantor-idFront.jpg",
    ...
  }
}
```

## 测试验证

### 测试步骤

1. **共借人页面测试**
   - 填写共借人信息（个人或对公）
   - 点击"保存"按钮
   - ✅ 应显示"新增成功"或"保存成功"
   - ✅ 1.5秒后自动返回上一页

2. **担保人页面测试**
   - 填写担保人信息（个人、对公或房产）
   - 点击"保存"按钮
   - ✅ 应显示"新增成功"或"保存成功"
   - ✅ 1.5秒后自动返回上一页

3. **编辑模式测试**
   - 进入已保存的共借人/担保人页面
   - 修改信息后点击"保存"
   - ✅ 应显示"更新成功"或"保存成功"
   - ✅ 1.5秒后自动返回上一页

### 验证要点

- ✅ 保存成功时显示正确的提示消息（来自后端的 `message` 字段）
- ✅ 保存失败时显示错误消息
- ✅ 网络错误时显示"保存失败，请重试"
- ✅ 保存成功后自动返回上一页
- ✅ 保存的数据能正确回显

## 相关文件

- `wx-miniapp/miniprogram/pages/order/create/co-borrower/index.js` - 共借人页面
- `wx-miniapp/miniprogram/pages/order/create/guarantor/index.js` - 担保人页面
- `wx-miniapp/miniprogram/utils/request.js` - 请求工具
- `src/main/java/com/example/loanminiapp/controller/OrderCoBorrowerController.java` - 共借人后端接口
- `src/main/java/com/example/loanminiapp/controller/OrderGuarantorController.java` - 担保人后端接口

## 注意事项

1. **request.js 的返回值**
   - 所有使用 `wx.$request` 的地方都需要注意这个问题
   - 建议统一处理：`const result = response.data;`

2. **变量命名规范**
   - 共借人：`coBorrowerData`, `_coBorrowerId`, `orderFormData_step3`
   - 担保人：`guarantorData`, `_guarantorId`, `orderFormData_step4`

3. **接口路径规范**
   - 共借人：`/public/orders/{orderId}/coBorrower/...`
   - 担保人：`/public/orders/{orderId}/guarantor/...`

## 修复日期

2026-02-05

