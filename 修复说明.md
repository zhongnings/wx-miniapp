# 共借人和担保人页面上传功能修复说明

## 问题描述

共借人和担保人页面中，以下图片上传功能存在问题：
1. **身份证正反面**：点击图片时只是预览，没有调用后台上传接口
2. **营业执照和房产证**：选择图片后只保存到本地临时路径，没有上传到服务器

## 修复内容

### 1. 共借人页面 (`co-borrower/index.js`)

#### 修复的方法：

1. **`uploadBusinessLicense()`** - 营业执照上传
   - 修复前：只选择图片，保存本地路径
   - 修复后：调用 `wx.$upload.uploadOrderFile()` 上传到服务器
   - 使用文件类型：`coBorrower-businessLicense`

2. **`uploadAgentIdCardFront()` / `uploadAgentIdCardBack()`** - 经办人身份证上传
   - 修复前：只选择图片，保存本地路径
   - 修复后：调用 `chooseIdCardImage()` 方法，支持上传+OCR识别
   - 传入 `personType='agent'` 参数区分经办人

3. **`chooseIdCardImage(field, imageType, personType)`** - 身份证选择和上传
   - 新增 `personType` 参数，支持区分共借人本人和经办人
   - 默认值：`'coBorrower'`（共借人本人）
   - 可选值：`'agent'`（经办人）

4. **`uploadIdCardImageWithOcr(imagePath, imageType, personType)`** - 身份证上传
   - 新增 `personType` 参数
   - 根据 `personType` 获取对应的身份证号
   - 调用 `wx.$upload.uploadIdCardImage()` 时传入正确的 `personType`：
     - 共借人本人：`'coBorrower'`
     - 经办人：`'coBorrower-agent'`

5. **`parseOcrResult(ocrData, side, personType)`** - OCR结果解析
   - 新增 `personType` 参数
   - 根据 `personType` 自动选择正确的字段前缀：
     - 共借人本人：无前缀（`name`, `idNumber`, `idAddress` 等）
     - 经办人：`agent` 前缀（`agentName`, `agentIdNumber`, `agentIdAddress` 等）

### 2. 担保人页面 (`guarantor/index.js`)

#### 修复的方法：

1. **`uploadBusinessLicense()`** - 营业执照/房产证上传
   - 修复前：只选择图片，保存本地路径
   - 修复后：调用 `wx.$upload.uploadOrderFile()` 上传到服务器
   - 根据类型自动选择文件类型：
     - 对公：`guarantor-businessLicense`
     - 房产：`guarantor-propertyLicense`

2. **`uploadAgentIdCardFront()` / `uploadAgentIdCardBack()`** - 经办人身份证上传
   - 修复前：只选择图片，保存本地路径
   - 修复后：调用 `chooseIdCardImage()` 方法，支持上传+OCR识别
   - 传入 `personType='agent'` 参数区分经办人

3. **`chooseIdCardImage(field, imageType, personType)`** - 身份证选择和上传
   - 新增 `personType` 参数，支持区分担保人本人和经办人
   - 默认值：`'guarantor'`（担保人本人）
   - 可选值：`'agent'`（经办人）

4. **`uploadIdCardImageWithOcr(imagePath, imageType, personType)`** - 身份证上传
   - 新增 `personType` 参数
   - 根据 `personType` 获取对应的身份证号
   - 调用 `wx.$upload.uploadIdCardImage()` 时传入正确的 `personType`：
     - 担保人本人：`'guarantor'`
     - 经办人：`'guarantor-agent'`

5. **`parseOcrResult(ocrData, side, personType)`** - OCR结果解析
   - 新增 `personType` 参数
   - 根据 `personType` 自动选择正确的字段前缀：
     - 担保人本人：无前缀（`name`, `idNumber`, `idAddress` 等）
     - 经办人：`agent` 前缀（`agentName`, `agentIdNumber`, `agentIdAddress` 等）

## 技术实现

### 1. 复用 step2 的上传逻辑

参考 `step2/index.js` 中的身份证上传实现：
- 选择图片 → 压缩（如果超过2MB）→ 上传到服务器 → OCR识别 → 填充表单
- 使用全局上传工具 `wx.$upload.uploadIdCardImage()`

### 2. 使用通用上传接口

营业执照和房产证使用 `wx.$upload.uploadOrderFile()` 方法：
- 参数1：图片本地路径
- 参数2：文件类型（用于区分不同业务场景）
- 参数3：订单ID

### 3. 后端接口调用

所有上传都会调用后端 `/public/upload` 接口：
- 身份证上传：
  - 参数：`orderId`, `idNumber`, `imageType`, `enableOcr`
  - 后端会自动进行OCR识别
  - 返回：`{ url, ocr, ocrSuccess }`

- 通用文件上传：
  - 参数：`orderId`, `bizType`
  - 不进行OCR识别
  - 返回：`{ url, name, size }`

## 测试要点

### 共借人页面测试：

1. **个人类型**：
   - ✅ 上传身份证正面 → 应调用上传接口 → 自动OCR识别
   - ✅ 上传身份证反面 → 应调用上传接口 → 自动OCR识别
   - ✅ 点击已上传的身份证 → 应预览图片（不是重新上传）

2. **对公类型**：
   - ✅ 上传营业执照 → 应调用上传接口
   - ✅ 上传经办人身份证正面 → 应调用上传接口 → 自动OCR识别（填充经办人字段）
   - ✅ 上传经办人身份证反面 → 应调用上传接口 → 自动OCR识别（填充经办人字段）

### 担保人页面测试：

1. **个人类型**：
   - ✅ 上传身份证正面 → 应调用上传接口 → 自动OCR识别
   - ✅ 上传身份证反面 → 应调用上传接口 → 自动OCR识别

2. **对公类型**：
   - ✅ 上传营业执照 → 应调用上传接口
   - ✅ 上传经办人身份证正面 → 应调用上传接口 → 自动OCR识别（填充经办人字段）
   - ✅ 上传经办人身份证反面 → 应调用上传接口 → 自动OCR识别（填充经办人字段）

3. **房产类型**：
   - ✅ 上传房产证 → 应调用上传接口
   - ✅ 上传经办人身份证正面 → 应调用上传接口 → 自动OCR识别（填充经办人字段）
   - ✅ 上传经办人身份证反面 → 应调用上传接口 → 自动OCR识别（填充经办人字段）

## 注意事项

1. **图片压缩**：超过2MB的图片会自动压缩到70%质量
2. **OCR识别**：身份证上传会自动进行OCR识别，识别失败不影响上传
3. **字段映射**：经办人的OCR结果会自动填充到带 `agent` 前缀的字段
4. **错误处理**：上传失败会显示错误提示，不会影响其他功能

## 相关文件

- `wx-miniapp/miniprogram/pages/order/create/co-borrower/index.js` - 共借人页面
- `wx-miniapp/miniprogram/pages/order/create/guarantor/index.js` - 担保人页面
- `wx-miniapp/miniprogram/utils/upload.js` - 上传工具类
- `src/main/java/com/example/loanminiapp/controller/UploadController.java` - 后端上传接口

## 修复日期

2026-02-05

