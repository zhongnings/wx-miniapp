# 共借人和担保人数据加载修复说明

## 问题描述

共借人和担保人页面在加载已有数据时，无法正确显示后端返回的数据。

## 问题原因

与保存功能相同的问题：`request.js` 返回的是**完整的响应对象** `{ statusCode, data }`，而不是直接返回 `data`。

在 `loadData()` 方法中，代码错误地判断了 `result.success`，但实际应该判断 `result.data.success`。

## 修复内容

### 1. 共借人页面 (`co-borrower/index.js`)

**修复前：**
```javascript
async loadData() {
  const req = wx.$request;
  const result = await req.get(`/public/orders/${orderId}/coBorrower/list`);
  
  // ❌ 错误：result.success 不存在
  if (result && result.success && result.data && Array.isArray(result.data)) {
    const coBorrower = result.data[0];
    // 加载数据...
  }
}
```

**修复后：**
```javascript
async loadData() {
  const req = wx.$request;
  const response = await req.get(`/public/orders/${orderId}/coBorrower/list`);
  
  // ✅ 正确：先取出 response.data
  const result = response.data;
  
  // ✅ 正确：result.success 存在
  if (result && result.success && result.data && Array.isArray(result.data)) {
    const coBorrower = result.data[0];
    // 加载数据...
  }
}
```

### 2. 担保人页面 (`guarantor/index.js`)

**修复内容相同：**
```javascript
async loadData() {
  const req = wx.$request;
  const response = await req.get(`/public/orders/${orderId}/guarantor/list`);
  
  // ✅ 正确：先取出 response.data
  const result = response.data;
  
  // ✅ 正确：result.success 存在
  if (result && result.success && result.data && Array.isArray(result.data)) {
    const guarantor = result.data[0];
    // 加载数据...
  }
}
```

## 数据流程

### 后端返回的数据结构

**共借人列表接口：** `GET /public/orders/{orderId}/coBorrower/list`

**担保人列表接口：** `GET /public/orders/{orderId}/guarantor/list`

**返回格式：**
```json
{
  "success": true,
  "data": [
    {
      "id": 1,
      "borrowerType": "personal",
      "name": "张三",
      "phone": "13800138000",
      "idNumber": "330102199001011234",
      "idCardFront": "http://localhost:8081/uploads/1/coBorrower-idFront.jpg",
      "idCardBack": "http://localhost:8081/uploads/1/coBorrower-idBack.jpg",
      ...
    }
  ],
  "total": 1
}
```

### request.js 返回的数据结构

```javascript
{
  statusCode: 200,
  data: {
    success: true,
    data: [ { id: 1, ... } ],
    total: 1
  }
}
```

### 正确的数据访问方式

```javascript
const response = await req.get('/api/xxx');  // 获取完整响应
const result = response.data;                 // 取出业务数据
const list = result.data;                     // 取出列表数据
const firstItem = list[0];                    // 取出第一项
```

## 功能说明

### 加载逻辑

1. **优先从后端加载**
   - 如果有 `orderId`，调用后端接口获取数据
   - 取列表中的第一项（当前只支持一个共借人/担保人）
   - 保存 ID 到 `this._coBorrowerId` 或 `this._guarantorId`
   - 映射后端数据到页面字段

2. **后端无数据时从本地加载**
   - 如果后端返回空列表，尝试从本地存储加载
   - 本地存储键名：
     - 共借人：`orderFormData_step3`
     - 担保人：`orderFormData_step4`

3. **加载失败时的降级处理**
   - 网络错误或接口异常时，自动降级到本地加载
   - 显示加载失败日志，但不影响用户操作

### 字段映射

后端字段和前端字段的映射关系：

| 后端字段 | 前端字段 | 说明 |
|---------|---------|------|
| `borrowerType` | `borrowerType` | 类型：personal/company/property |
| `name` | `name` | 姓名 |
| `phone` | `phone` | 手机号 |
| `idNumber` | `idNumber` | 身份证号 |
| `idCardFront` | `idCardFront` | 身份证正面URL |
| `idCardBack` | `idCardBack` | 身份证反面URL |
| `idStartDate` | `idStartDate` | 证件生效日期 |
| `idEndDate` | `idEndDate` | 证件有效期 |
| `idAddress` | `idAddress` | 证件地址 |
| `residenceArea` | `residenceArea` | 居住地省市区 |
| `detailAddress` | `detailAddress` | 详细地址 |
| `relationship` | `relationship` | 与主借人关系 |
| `maritalStatus` | `maritalStatus` | 婚姻状况 |
| `businessLicense` | `businessLicense` | 营业执照/房产证URL |
| `companyName` | `companyName` | 公司名称 |
| `companyCreditCode` | `companyCreditCode` | 公司信用代码/房产证号 |
| `companyArea` | `companyArea` | 注册地/房产地省市区 |
| `companyAddress` | `companyAddress` | 公司/房产详细地址 |
| `agentName` | `agentName` | 经办人姓名 |
| `agentPhone` | `agentPhone` | 经办人手机号 |
| `agentIdNumber` | `agentIdNumber` | 经办人身份证号 |
| `agentIdCardFront` | `agentIdCardFront` | 经办人身份证正面URL |
| `agentIdCardBack` | `agentIdCardBack` | 经办人身份证反面URL |
| `notaryDocuments` | `notaryDocuments` | 公证材料列表 |

## 测试验证

### 测试场景

1. **首次进入页面（无数据）**
   - ✅ 显示空表单
   - ✅ 可以正常填写和保存

2. **再次进入页面（有数据）**
   - ✅ 自动加载并显示已保存的数据
   - ✅ 身份证图片正确显示
   - ✅ 所有字段正确回显
   - ✅ 可以修改并更新数据

3. **网络异常情况**
   - ✅ 显示"加载中..."提示
   - ✅ 加载失败后自动降级到本地存储
   - ✅ 不影响用户继续操作

4. **编辑模式**
   - ✅ 修改数据后点击保存
   - ✅ 调用更新接口（PUT）而不是新增接口（POST）
   - ✅ 保存成功后数据正确更新

### 验证要点

- ✅ 个人类型：身份证图片、姓名、手机号、身份证号等字段正确回显
- ✅ 对公类型：营业执照、公司信息、经办人信息正确回显
- ✅ 房产类型（仅担保人）：房产证、房产信息、经办人信息正确回显
- ✅ 公证材料列表正确显示
- ✅ 编辑后保存，数据正确更新

## 相关修复

本次修复涉及以下方法：

### 共借人页面
- ✅ `loadData()` - 加载数据
- ✅ `save()` - 保存数据（之前已修复）

### 担保人页面
- ✅ `loadData()` - 加载数据
- ✅ `save()` - 保存数据（之前已修复）

## 相关文件

- `wx-miniapp/miniprogram/pages/order/create/co-borrower/index.js` - 共借人页面
- `wx-miniapp/miniprogram/pages/order/create/guarantor/index.js` - 担保人页面
- `wx-miniapp/miniprogram/utils/request.js` - 请求工具
- `src/main/java/com/example/loanminiapp/controller/OrderCoBorrowerController.java` - 共借人后端接口
- `src/main/java/com/example/loanminiapp/controller/OrderGuarantorController.java` - 担保人后端接口

## 注意事项

1. **统一的响应处理**
   - 所有使用 `wx.$request` 的地方都需要先取 `response.data`
   - 建议在项目中统一处理，避免类似问题

2. **ID 的保存**
   - 加载数据时保存 `_coBorrowerId` 或 `_guarantorId`
   - 保存时根据是否有 ID 判断是新增还是更新

3. **降级策略**
   - 后端加载失败时自动降级到本地存储
   - 确保用户体验不受网络影响

## 修复日期

2026-02-05

