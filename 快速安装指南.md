# Java 和 Maven 快速安装指南

## 📋 系统要求

- **操作系统**: Windows 10/11
- **需要的软件**: Java 8 (JDK 1.8) + Maven 3.6+
- **磁盘空间**: 至少 500MB
- **网络**: 需要下载约 200MB 文件

---

## 🚀 方法一：使用 Chocolatey 安装（最简单）

### 步骤 1: 以管理员身份打开 PowerShell

右键点击"开始菜单" → 选择"Windows PowerShell (管理员)"

### 步骤 2: 安装 Chocolatey

```powershell
Set-ExecutionPolicy Bypass -Scope Process -Force
[System.Net.ServicePointManager]::SecurityProtocol = [System.Net.ServicePointManager]::SecurityProtocol -bor 3072
iex ((New-Object System.Net.WebClient).DownloadString('https://community.chocolatey.org/install.ps1'))
```

### 步骤 3: 安装 Java 和 Maven

```powershell
# 安装 Java 8
choco install openjdk8 -y

# 安装 Maven
choco install maven -y
```

### 步骤 4: 验证安装

**关闭并重新打开 PowerShell**，然后运行：

```powershell
java -version
mvn -version
```

✅ 如果看到版本信息，说明安装成功！

---

## 🔧 方法二：使用自动安装脚本

### 步骤 1: 进入项目目录

```powershell
cd E:\workspace\miniapp
```

### 步骤 2: 允许脚本执行

```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### 步骤 3: 运行安装脚本

```powershell
.\install-java-maven.ps1
```

脚本会自动：
- ✅ 下载 OpenJDK 8
- ✅ 下载 Apache Maven 3.9.6
- ✅ 解压到 `C:\DevTools` 目录
- ✅ 配置环境变量
- ✅ 配置 Maven 阿里云镜像

### 步骤 4: 重启 PowerShell 并验证

```powershell
java -version
mvn -version
```

---

## 📦 方法三：手动安装

### 安装 Java JDK 8

1. **下载 JDK 8**
   - 访问: https://adoptium.net/temurin/releases/?version=8
   - 选择: Windows x64 的 `.msi` 安装包
   - 或者: https://www.oracle.com/java/technologies/javase/javase8-archive-downloads.html

2. **安装 JDK**
   - 双击 `.msi` 文件
   - 按照向导安装（默认路径：`C:\Program Files\Java\jdk1.8.0_xxx`）

3. **配置环境变量**
   - 右键"此电脑" → "属性" → "高级系统设置" → "环境变量"
   - 新建系统变量：
     - 变量名: `JAVA_HOME`
     - 变量值: `C:\Program Files\Java\jdk1.8.0_xxx`（你的实际安装路径）
   - 编辑系统变量 `Path`，添加: `%JAVA_HOME%\bin`

### 安装 Maven

1. **下载 Maven**
   - 访问: https://maven.apache.org/download.cgi
   - 下载: `apache-maven-3.9.6-bin.zip`

2. **解压 Maven**
   - 解压到: `C:\Program Files\Apache\maven`

3. **配置环境变量**
   - 新建系统变量：
     - 变量名: `MAVEN_HOME`
     - 变量值: `C:\Program Files\Apache\maven`
   - 编辑系统变量 `Path`，添加: `%MAVEN_HOME%\bin`

4. **配置 Maven 镜像（可选但推荐）**
   - 打开文件: `C:\Program Files\Apache\maven\conf\settings.xml`
   - 在 `<mirrors>` 标签内添加：

```xml
<mirror>
  <id>aliyun</id>
  <mirrorOf>central</mirrorOf>
  <name>阿里云 Maven 镜像</name>
  <url>https://maven.aliyun.com/repository/public</url>
</mirror>
```

---

## ✅ 验证安装

### 运行检查脚本

```powershell
cd E:\workspace\miniapp
.\check-env.ps1
```

### 或手动检查

```powershell
# 检查 Java
java -version
# 应该显示: java version "1.8.0_xxx"

# 检查 Maven
mvn -version
# 应该显示: Apache Maven 3.9.6

# 检查环境变量
echo $env:JAVA_HOME
echo $env:MAVEN_HOME
```

---

## 🏗️ 构建项目

环境配置成功后，可以构建项目：

```powershell
# 进入项目目录
cd E:\workspace\miniapp

# 清理并编译
mvn clean compile

# 打包（跳过测试）
mvn clean package -DskipTests

# 运行项目
mvn spring-boot:run

# 生成开发环境包
mvn clean package -Pdev

# 生成生产环境包
mvn clean package -Pprod
```

---

## ❓ 常见问题

### 问题 1: 命令找不到

**现象**: 运行 `java` 或 `mvn` 提示"无法识别"

**解决方案**:
1. 确认环境变量配置正确
2. **重启 PowerShell 或 IDE**（必须重启才能加载新的环境变量）
3. 如果还不行，重启电脑

### 问题 2: 中文乱码

**现象**: PowerShell 显示乱码

**解决方案**:
```powershell
# 设置控制台编码为 UTF-8
chcp 65001
```

或在脚本开头添加：
```powershell
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8
```

### 问题 3: JAVA_HOME 未设置

**现象**: Maven 提示 "JAVA_HOME is not set"

**解决方案**:
```powershell
# 临时设置（当前会话）
$env:JAVA_HOME = "C:\Program Files\Java\jdk1.8.0_xxx"

# 永久设置（需要管理员权限）
[System.Environment]::SetEnvironmentVariable("JAVA_HOME", "C:\Program Files\Java\jdk1.8.0_xxx", "Machine")
```

### 问题 4: Maven 下载依赖很慢

**解决方案**:
1. 配置阿里云镜像（见上文）
2. 或使用代理：
```powershell
mvn clean package -DskipTests -Dhttp.proxyHost=127.0.0.1 -Dhttp.proxyPort=7890
```

### 问题 5: 权限不足

**现象**: 提示"拒绝访问"或"权限不足"

**解决方案**:
- 以管理员身份运行 PowerShell
- 右键"开始菜单" → "Windows PowerShell (管理员)"

---

## 📞 获取帮助

如果遇到问题，请提供以下信息：

1. 运行 `.\check-env.ps1` 的完整输出
2. 运行 `java -version` 和 `mvn -version` 的输出
3. 完整的错误信息截图

---

## 📚 相关文档

- `JAVA_MAVEN_SETUP.md` - 详细的安装和配置文档
- `check-env.ps1` - 环境检查脚本
- `install-java-maven.ps1` - 自动安装脚本
- `pom.xml` - Maven 项目配置文件

---

## 🎯 下一步

环境配置完成后，您可以：

1. ✅ 编译项目: `mvn clean compile`
2. ✅ 运行测试: `mvn test`
3. ✅ 打包应用: `mvn clean package`
4. ✅ 启动应用: `mvn spring-boot:run`
5. ✅ 查看日志: `logs/app.log`

祝您开发顺利！🚀


















